apiVersion: v1
kind: Secret
metadata:
  name: cinellex-secrets
  namespace: cinellex-staging
type: Opaque
stringData:
  GROQ_API_KEY: "REPLACE_ME"
  # TMDB v3 API key OR v4 Read Access Token — the app's live movie data source.
  TMDB_API_KEY: "REPLACE_ME"

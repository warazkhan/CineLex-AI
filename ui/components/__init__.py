"""Reusable Streamlit view components for the CineLex UI.

Each module renders one section of the page and owns only that section's
markup. State lives in ``st.session_state`` (seeded by ``ui.state``).
"""

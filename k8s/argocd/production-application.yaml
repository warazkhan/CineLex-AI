apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cinellex-ai-production
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/warazkhan/CineLex-AI
    targetRevision: gitops        # watches gitops branch now
    path: k8s/production          # production manifests
    directory:
      recurse: false
  destination:
    server: https://kubernetes.default.svc
    namespace: cinellex-production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
